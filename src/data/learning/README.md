# Learning modules (from IBIG PDFs)

Content here is filled from **Desktop/Bread App/Bread/Content/**:

- **modules-index.json** — List of all modules; maps to `quizTopic` and source PDF names.
- **\<slug>.json** — One file per module (e.g. `accounting-3-statements.json`). Schema: `src/types/learning.ts` (`LearningModule`).

Copy or convert each IBIG PDF into a matching JSON. See project root **CONTENT_INTEGRATION.md** for the full process.
